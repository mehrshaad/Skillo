node_modules
.wxt
.output
stats.html
*.log
.DS_Store

# Scratch tooling for picking a design, kept locally but not part of the repo.
# icon20.png is NOT here: it is the icon master that scripts/generate-icons.mjs
# builds from, so it has to be tracked.
icon-options/
scripts/generate-icon-options.mjs

# Raw store captures hold a real resume and a real name. The framed versions in
# store/ are reviewed before they go out; these never belong in a public repo.
store/raw/
